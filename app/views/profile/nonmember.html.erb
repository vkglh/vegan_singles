<section class="section fullheight bg-blue-navy dark padding-v-100" data-header-change="true">
        
        <div class="section-content v-center">
            <div class="border-bottom margin-b-80">
                <div class="container container-wide">
                    <h1 class="text-center"><i class="fa fa-user" style='margin-right: 10px;'> </i></a> Membership Required</h1>
                </div>
            </div>
            
            <div class="container">
                    <center>
                    <h3>You must be a member to view profiles.</h3>
                    <% if current_user == nil %>
                        <a href="/login" class="btn btn-mint-green btn-filled"><span>Log In</span></a>
                    <% else %>
                        <a href="/membership/new" class="btn btn-mint-green btn-filled"><span>Subscribe</span></a>
                    <% end %>
                    </center>
            </div>
        </div>

</section>
